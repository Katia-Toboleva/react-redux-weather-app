import React from 'react';
import styles from './location-input.scss';

const LocationInput = () => (
  <input className={styles['location-input']} placeholder="Location..." />
);

export default LocationInput;
